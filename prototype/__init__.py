"""land-dev-prototype: Phase 0 scaffold of the AI land-development CAD agent.

Deterministic pipeline: parcel -> candidate schemes -> validation ->
pro forma -> comparison -> DXF. No LLM geometry (per build plan: geometry
lives in deterministic engines, outside token loops).
"""

__version__ = "0.1.0"
