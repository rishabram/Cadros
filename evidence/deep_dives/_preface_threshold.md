## Deep-dive narrative — archetype: barely subdividable (threshold)

> **Machine-generated narrative (machine-draft — not human-verified).** Every
> figure below is copied from this parcel's `report.json` and from
> `screening_results.csv`; no new facts, no verdicts, no recommendations.

**Why this parcel — selection documented:** the brief asked for a top scheme
of 1–2 lots. **No scheme in the 500-parcel screen has 1–2 lots, and the
smallest top scheme anywhere is 4 lots.** This parcel was therefore chosen as
the **thinnest-margin 4-lot top scheme in the screen** — the closest the data
gets to the threshold archetype.

- Parcel `08253290060000` — **BP** (Business Park), **3.41 acres**. The
  pipeline produced **exactly one scheme** (`scheme_00`, geometry-clean):
  **4 lots**, profit **$184,482.75**, margin **34.16%**.

**Why this is the threshold case:**

1. **One scheme, no fallback.** There is no second layout to retreat to if
   the single scheme's assumptions slip — ranking, sensitivity, and option
   value are all absent by construction.
2. **Thinnest cushion in its cohort.** At 34.16% margin on assumption-grade
   economics, small input moves erase the profit: the economics are
   **DEFAULT-fallback** ("Valley-average Wasatch Front finished SFR lot;
   judgment, no comp pulled"), applied to a BP parcel.
3. **Provisional geometry.** The 4 lots were sized against provisional BP
   dimensionals, and — as on every non-MU-11/non-PL parcel — the RuleGraph
   PASSes are all `applicable: false`: **no BP rule has been human-verified**,
   so compliance is entirely unchecked.
4. **Use-allowance pending.** Whether the intended use is allowed in BP under
   21A.33 has not been evaluated (checker not yet integrated).

**Threshold logic, plainly:** a 4-lot, single-scheme, thin-margin parcel is
where screening precision matters most and where this screen has the least of
it. The $184,482.75 is better read as "worth one careful manual look" than as
a viable project — the first verified BP dimensional or the first real comp
could move it either side of zero.
